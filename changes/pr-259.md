[feature] Grid 2.0 — order-of-magnitude better battery via activity-aware location throttle, home auto-pause finally works, peer speed and battery now surface in the info bubble, freshness-based avatar status (live / stale / idle), redesigned sharing windows (list, edit, toggle, delete), on-device reverse geocoding, plus a full UI refresh across onboarding, contacts, groups, settings, and notifications.
